# Migrations

The production persistence migration is intentionally the next implementation increment. The running demo uses deterministic in-memory state so presentations do not require network API keys. Target database entities are documented in the root README and include PostGIS `street_segments`, POIs, segment scores, trips, events, positions, and reports.
